post:
  description: >-
    Generate new Basket comparison document associated with Application.
  summary: Request Generation of new Basket comparison document associated with Application
  tags:
  - Basket
  operationId: Request Generation of new Basket comparison document associated with Application
  produces:
  - application/json
  parameters:
  - name: applicationId
    in: path
    required: true
    type: string
    description: Unique ID of existing Application.
  responses:
    200:
      description: ''
      schema:
        $ref: '../swagger.yml#/definitions/Document'
    404:
      description: 'Request Generation of new Basket comparison document for Application without Products in the Basket.'
  x-operation-settings:
    CollectParameters: false
    AllowDynamicQueryParameters: false
    AllowDynamicFormParameters: false
    IsMultiContentStreaming: false
