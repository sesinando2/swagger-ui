title: CustomerDecision
type: object
properties:
  customer:
    $ref: '#/definitions/DecisionCustomer'
  decisions:
    type: array
    description: List of Product Component Decisions.
    items:
      $ref: '#/definitions/ComponentDecision'
  productExclusionContributions:
    type: array
    description: List of Product Decisions. Mutually exclusive to exclusionContributions.
    items:
      $ref: '#/definitions/ExclusionOptions'
required:
- customer
- decisions
